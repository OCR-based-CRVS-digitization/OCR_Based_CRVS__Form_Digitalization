{
  "scripts": {
    "start" : "node ./index",
    "dev" : "nodemon ./index",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "latest",
    "express-fileupload": "^1.4.0",
    "jsonwebtoken": "latest",
    "pg": "latest",
    "prompt": "latest"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
